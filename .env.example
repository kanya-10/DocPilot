# Copy this file to .env and fill in your values.

# Required
GROQ_API_KEY=gsk_...

# Vector store: "faiss" (default, free, local) or "pinecone" (production)
VECTOR_STORE_BACKEND=faiss

# Only required if VECTOR_STORE_BACKEND=pinecone
PINECONE_API_KEY=
PINECONE_INDEX_NAME=docpilot
